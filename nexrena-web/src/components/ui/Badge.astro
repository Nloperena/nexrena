---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---
<span class={`inline-flex items-center px-4 py-1.5 rounded-full border border-[var(--gold)] font-mono text-[11px] uppercase tracking-widest text-[var(--gold)] ${className}`}>
  <slot />
</span>
