---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---
<span class={`font-mono text-[11px] text-[var(--gold-dim)] tracking-widest uppercase block ${className}`}>
  <slot />
</span>
