{
  "name": "nexrena",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-postbuild": "npm install --prefix nexrena-api --include=dev && npm run build --prefix nexrena-api"
  }
}
