# PostgreSQL â€” set by Heroku automatically, or use a local connection string
DATABASE_URL="postgresql://user:password@localhost:5432/nexrena_dev?schema=public"

# Secret key for Ops dashboard API requests (generate any random string)
API_KEY="change-me-to-a-random-secret"

# Comma-separated allowed origins for CORS
CORS_ORIGINS="http://localhost:3001,http://localhost:4321,https://nexrena.com,https://nexrena-ops.vercel.app"

# Port â€” Heroku sets this automatically
PORT=4000
